# Model registry for anomaly detection (tool-agnostic)
# Paths are relative to the current working directory

# List of models to run (ECG and AUX)
models=ecg,aux

# Execution order - ECG runs first, then AUX
order=ecg,aux

# ECG model configuration (existing float32 model)
ecg.name=ecg_model
ecg.path=models/ecg_model_float32.onnx

# AUX model configuration (placeholder for second model)
aux.name=aux_model
aux.path=models/cnn_ecg_quant
